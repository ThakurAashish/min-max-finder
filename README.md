# min-max-finder
This algorithm scans the array only once (single loop), comparing each element to update minimum and maximum values. It works in O(n) time, where n is the number of elements. It is an efficient way to find min and max because it requires only simple comparisons
